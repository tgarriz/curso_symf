{% extends 'base.html.twig' %}

{% block body %}
    <h1>Origen</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ origen.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ origen.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('origen_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('origen_edit', { 'id': origen.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
